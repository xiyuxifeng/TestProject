name: Test Schedule
on:
  schedule:
     - cron: '15 * * * *'
# on:
#   push:
#     branches: [ "master" ]

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: echo time
        run: echo $(date)
